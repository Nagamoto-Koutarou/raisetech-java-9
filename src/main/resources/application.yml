#データソース
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/name_db
    username: root
    password: ${password:password}
    driver-class-name: com.mysql.cj.jdbc.Driver
    jackson:
      serialization:
        fail-on-empty-beans: false

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rasetech.java9.mapper.CoffeeMapper">
    <select id="findAll" resultType="com.rasetech.java9.entity.Coffee">
        SELECT
            id, productName, countryOfOrigin, degreeOfRoasting, aroma, acidity, bitterTaste, sweetTaste, rich, priceOf100g, priceOf200g
        FROM
            coffees
        ORDER BY
            id
    </select>
    <select id="findById" resultType="com.rasetech.java9.entity.Coffee">
        SELECT
            id,productName, countryOfOrigin, degreeOfRoasting, aroma, acidity, bitterTaste, sweetTaste, rich, priceOf100g, priceOf200g
        FROM
            coffees
        WHERE
            id = #{id}
    </select>
</mapper>
